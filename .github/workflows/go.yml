name: Go
on: [push]
jobs:

  build:
    name: Build&test
    runs-on: ubuntu-latest
    steps:

    - name: Check out code
      uses: actions/checkout@v3

    - name: Set up Go 1.20
      uses: actions/setup-go@v4
      with:
        go-version: 1.20.2
      id: go

    - name: Build
      run: go build -v .

    - name: Test
      run: go test -v ./...
